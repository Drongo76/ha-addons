name: "Tado Assistant (Ingress)"
slug: "tado_assistant"
description: "Tado Assistant as HA add-on with web UI (Ingress)."
version: "0.1.0"
arch:
  - aarch64
  - amd64
  - armv7
startup: application
boot: auto
init: false

ingress: true
ingress_port: 8099
panel_icon: "mdi:radiator"
panel_title: "Tado Assistant"

options: {}
schema: {}
